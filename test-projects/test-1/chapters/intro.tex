\section{TexForge}

TexForge is a local-first LaTeX IDE running in the browser.

The filesystem on the local machine is the source of truth.

This file is intentionally stored inside a subdirectory so that we can test recursive filesystem traversal.
